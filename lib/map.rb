class Map
  def initialize(rows, columns, vehicles, rides, bonus, steps)
    @rows = rows
    @columns = columns
    @vehicles = vehicles
    @rides = rides
    @bonus = bonus
    @steps = steps
  end
end
